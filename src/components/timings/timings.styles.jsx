import {StyleSheet} from  'react-native'
import {spacing} from '../../styles/sizes'

export const timingsStyles = StyleSheet.create({
  container:{
    display:'flex',
    flexDirection:'row',
    justifyContent:'space-between',
    paddingTop: spacing.md,
    paddingBottom: spacing.md,
  }
})